<Page x:Class="LibraLibraryManagementSystem.Controls.BtnEditBook"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignWidth="800"
      d:DesignHeight="450"
      FontFamily="Montserrat"
      Background="White">

    <Page.Resources>
        <!-- RoundedButton Style -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#57B894"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.95"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D0D6DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#A0AFC0"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#57B894"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox style with explicit key so StaticResource lookup succeeds -->
        <Style x:Key="ComboBoxRoundedStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D0D6DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="MinHeight" Value="27"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Main Border with rounded corners -->
                            <Border Name="MainBorder"
                            CornerRadius="20" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- ToggleButton (invisible, just for functionality) -->
                                    <ToggleButton Name="ToggleButton"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                Focusable="False"
                                ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"
                                                BorderBrush="#D0D6DB"
                                                BorderThickness="1"
                                                CornerRadius="20"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <!-- Content area -->
                                    <ContentPresenter Grid.Column="0"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="12,0,0,0"/>

                                    <!-- Dropdown arrow on the right -->
                                    <Path Grid.Column="1"
                                  IsHitTestVisible="False"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Margin="8,0,12,0"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="#D0D6DB"/>
                                </Grid>
                            </Border>

                            <!-- Drop-down popup -->
                            <Popup Name="Popup"
                        Placement="Bottom"
                        IsOpen="{TemplateBinding IsDropDownOpen}"
                        AllowsTransparency="True"
                        Focusable="False"
                        PopupAnimation="Slide">
                                <Border Background="White"
                            BorderBrush="#D0D6DB"
                            BorderThickness="1"
                            CornerRadius="12"
                            MinWidth="{TemplateBinding ActualWidth}"
                            MaxHeight="200">
                                    <ScrollViewer Margin="0,4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="ScaleRoot">
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="PageScale"/>
        </Grid.LayoutTransform>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Title -->
                <RowDefinition Height="12"/>
                <!-- Spacer -->
                <RowDefinition Height="*"/>
                <!-- Form -->
                <RowDefinition Height="Auto"/>
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock x:Name="txtEditTitle"
                Grid.Row="0"
                FontSize="24"
                FontWeight="Bold">
                    <Run Text="EDIT "/>
                    <Run Text="{Binding Title}"/>
            </TextBlock>

            <!-- Two-column form -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT COLUMN -->
                <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="0,0,18,0">
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="BookID"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <TextBox x:Name="txtBookID" IsReadOnly="True"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="Author"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <TextBox x:Name="txtAuthor"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="Title"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <TextBox x:Name="txtTitle"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Edition" FontWeight="SemiBold" FontSize="13"/>
                        <TextBox x:Name="txtEdition"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Volumes" FontWeight="SemiBold" FontSize="13"/>
                        <TextBox x:Name="txtVolumes"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Pages" FontWeight="SemiBold" FontSize="13"/>
                        <TextBox x:Name="txtPages"/>
                    </StackPanel>
                </StackPanel>

                <!-- RIGHT COLUMN -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="Publisher"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <TextBox x:Name="txtPublisher"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="Category"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <TextBox x:Name="txtCategory"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Year" FontWeight="SemiBold" FontSize="13"/>
                        <TextBox x:Name="txtYear"/>
                    </StackPanel>



                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="Location (shelf)"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <TextBox x:Name="txtLocation"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <Run Text="Status"/>
                            <Run Text=" *" Foreground="Red"/>
                        </TextBlock>
                        <ComboBox x:Name="cmbStatus"
                        ItemsSource="{Binding StatusOptions, RelativeSource={RelativeSource AncestorType=Page}}"
                        SelectedItem="{Binding Status, Mode=TwoWay}"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource ComboBoxRoundedStyle}"/>
                    </StackPanel>

                </StackPanel>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,10,0,0">
                <Button Content="UPDATE"
                        Background="#57B894"
                        Style="{StaticResource RoundedButton}"
                        Click="EditButton_Click"/>
                <Button Content="CANCEL"
                        Background="#555"
                        Style="{StaticResource RoundedButton}"
                        Margin="10,0,0,0"
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>